# 資料品質與爬蟲優化驗證紀錄

正式網域首頁於 2026-09-03 可正常載入，且正式 `GET /api/matching-rules` 已回傳 `rules` 與 `negativeFeatures` 欄位。後者目前為空陣列，這符合「至少兩次獨立人工拒絕後才匯出有限分數懲罰」的設計，而非端點遺漏欄位。

由於正式環境的手動拒絕會永久累積真實的負向特徵訓練資料，驗證未以假候選觸發該寫入。改以管理員 tRPC 契約測試驗證呼叫者身分、來源／候選／平台欄位與回傳的學習特徵數，並以 React 工作台可靠性測試驗證拒絕 mutation 的前端接線。正式無頭瀏覽器的受保護 `/review-queue` 在未具可用互動登入會話時呈現零內容，屬驗證環境會話限制；公開端點與開發工作台皆未出現本輪功能的執行錯誤。
