# 首頁上架率與同步控制檢查

## 正式站觀察

2026-09-03，以已登入管理員工作階段開啟正式首頁。頁面成功顯示完成批次 `#450001`、更新時間、待審核摘要與「重新載入資料」控制。

同日以帶有 `desktop-refresh-verification=1` 的新頁面載入首頁時，頁面先呈現「正在同步最新資料」與載入骨架；約十秒內完成，並回復「已同步完成批次 #450001」。這確認桌機首頁的資料重取狀態轉換與完成批次顯示皆可運作。

分類表的純文字擷取把同一平台的「已上架品數」和相鄰「上架率」連接，例如光碟燒錄機的 `7` 與 `87.5%` 被輸出為 `787.5%`。資料庫針對該分類的核對結果為：8 件欣亞商品、7 件原價屋配對、3 件 PChome 配對、4 件 momo 配對；因此合理百分比分別為 87.5%、37.5%、50.0%。

> 後續應改善分類欄內的品數／百分比視覺分隔，使行動與文字輔助工具使用者不會誤讀為超過 100%，但不可改變既有正確的後端百分比單位。
