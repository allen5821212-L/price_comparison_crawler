# 正式站驗證紀錄

- 管理員帳號已成功登入正式站，`/rules` 管理頁可載入，包含列表、搜尋、狀態篩選、套用次數、最後命中與啟用開關。
- 初次驗證發現列表中的「標記配對正確」只儲存本機標記，未同步規則；已修正並部署版本 `232ae8d9`。
- 已在正式站以三星 870 EVO 4TB 的 98% 相似度配對重新觸發直接確認，下一步需確認規則已出現在管理頁並驗證啟用／停用回寫。
- 點選管理連結後目前瀏覽器工作階段暫時跳轉至空白頁，後續將以正式站網址直接載入管理頁完成驗證。
- 直接載入後管理頁可在管理員登入狀態完整呈現，但仍顯示 0 筆同步規則；需檢查直接確認的前端 API 呼叫是否實際送出或是否遭伺服器拒絕。
- 已確認正式站載入的前端版本包含直接確認同步 mutation；並以管理員會話重現相同 payload，`matchRules.confirm` 成功回傳 200 並寫入三星 870 EVO 4TB 規則。
- 正式站管理頁目前已顯示該三星規則：總數 1、啟用 1、套用次數 0、最後命中為尚未套用，直接確認的同步規則流程已成功驗證。
- 已在正式站管理頁成功將三星規則切換為停用，啟用數即時降為 0；接著將由比價列表的直接確認重新啟用，驗證兩端狀態同步。
- 已在正式站透過實際「標記配對正確」按鈕重新確認三星配對，瀏覽器已觀測到 `matchRules.confirm` 請求送出；下一步確認管理頁恢復啟用狀態。
- 重新載入管理頁後，三星規則已恢復啟用，正式驗證了規則列表、停用、直接確認同步與重新啟用的完整生命週期。
- 已透過爬蟲使用統計回報流程套用三星規則一次；正式站管理頁顯示累積自動套用 1、該規則套用次數 1，並呈現最後命中時間。
