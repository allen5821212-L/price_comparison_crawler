# 資料新鮮度驗證紀錄

## 正式站基準狀態

- 驗證網址：https://pricecomp-cr-mlsxyggu.manus.space/comparisons
- 建立基準時間：2026-08-23 18:01 UTC
- 畫面完成批次：`#420001`
- 資料更新時間：`2026-08-23 17:15:29`
- 配對總數：`1,990`
- 可見範例商品：`ASUS ROG Strix SCAR 18 G835LXG-0081A290HX-NBLM`，欣亞 `NT$245,999`、原價屋 `NT$179,999`、momo `NT$179,999`。

## 觀察條件

正式瀏覽器已在排程工作 `#450002` 執行期間開啟 `/comparisons`。此工作應產生比 `#420001` 更新的 completed batch；列表的 10 秒狀態輪詢應在不導航、不使用重新載入按鈕的條件下自動重抓既有篩選／分頁資料。

## 實際同步結果

排程工作 `#450002` 於 `2026-08-23 19:33:42 UTC` 完成，寫入新完成批次 `#420002`。正式瀏覽器保留在既有的 `/comparisons` 列表，未點擊「重新載入」按鈕；之後以僅變更 URL 雜湊片段的方式讀取既有 DOM，畫面已顯示「已同步完成批次 #420002」、更新時間 `2026-08-23 19:33:36` 與 `1,994` 組配對。

可辨識的資料變化包括 TP-LINK Deco BE85 的 momo 價格從基準批次中沒有結果，更新為 `NT$2,599`；原比較列表由 `1,990` 組配對增加至 `1,994` 組。這證實完成批次輪詢會在無手動資料重新載入的情況下使產品價格與配對資料更新。

行動版檢查亦已確認首頁顯示最新時間 `2026-08-23 19:33:36` 與欣亞 `5,949` 項商品；行動版商品比價列表顯示 completed batch `#420002`、`1,994` 組配對、新資料時間及可操作的重新載入入口。

首頁已補上清楚的同步狀態提示。桌機頁首與主視覺都顯示「已同步完成批次 #420002」，行動版主視覺同樣可見該提示；兩種版面仍保留資料時間與重新載入圖示入口。

正式首頁以版本識別參數重新載入後，同樣顯示「已同步完成批次 #420002」於頁首及主視覺，並在初始查詢期間顯示「正在同步最新資料」。這確認正式部署的同步提示已生效。

## 首頁實際自動同步結果

為驗證首頁不需手動重新整理，已開啟完成批次 `#420002` 的正式首頁後，透過既有排程服務安全排入完整更新工作 `#450003`。該工作於 `2026-08-23 22:38:23 UTC` 完成，產生完成批次 `#420003`。保留既有首頁頁面且未點擊重新載入後，首頁自動顯示「已同步完成批次 #420003」、新的更新時間 `2026-08-23 22:38:17`，且 PChome 商品目錄數由 `8,673` 變為 `8,680`、momo 商品目錄數由 `4,248` 變為 `4,327`。

此結果與已驗證的商品比價列表自動同步共同證實：儀表板與商品比價列表都會在新 completed batch 寫入後自行載入最新資料，無需使用者手動重新載入資料。

## 立即重新載入入口

正式首頁的「重新載入資料」按鈕已實際操作：提示先切換為「正在同步最新資料」，資料重取完成後回復「已同步完成批次 #420003」。正式商品比價列表亦顯示相同的 completed batch、更新時間與「重新載入」入口。

行動版的版面與同步狀態已由 375×812 本機預覽驗證；嘗試使用獨立無頭瀏覽器對正式站進行行動版端對端觀察時，因該瀏覽器不繼承互動瀏覽器的登入工作階段而導向登入頁，故未將該探針列為正式行動版自動同步成功證據。

## 使用者行動版端對端確認

使用者於已登入手機上開啟 `/comparisons` 後保持頁面開啟。透過既有排程佇列服務建立完整更新工作 `#480001`；該工作於 `2026-08-24 10:27:08 UTC` 完成並寫入完成批次 `#450001`。使用者在未手動重新整理頁面的前提下回報行動版列表已自行顯示批次 `#450001`。

本批次可核對的真實資料差異包括：XPG VENTO 120 ARGB 白三入組的 momo 價格從 `NT$980` 變為 `NT$249`；微星 PRO B850-P WIFI 的 momo 價格從 `NT$8,490` 變為 `NT$5,890`。這補足了行動版正式端對端的完成批次自動同步證據。
